@model IEnumerable<WagenparkMVC.Models.werkplaat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.naam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.naam)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.werkplaatnr }) |
            @Html.ActionLink("Details", "Details", new { id=item.werkplaatnr }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.werkplaatnr })
        </td>
    </tr>
}

</table>
