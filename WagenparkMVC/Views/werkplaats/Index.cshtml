@model IEnumerable<WagenparkMVC.Models.werkplaat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.naam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.werkplaatsnr)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.naam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.werkplaatsnr)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.werkplaatsnr }) |
            @Html.ActionLink("Details", "Details", new { id=item.werkplaatsnr }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.werkplaatsnr })
        </td>
    </tr>
}

</table>
